import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/3d-chess-game/', // Ensures assets are linked relatively for GitHub Pages
})
